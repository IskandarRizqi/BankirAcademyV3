Coming soon
{{-- <form action="/set-master-refferal" method="POST">
    @csrf
    <input type="text" name="id" id="id" class="form-control" value="{{$reff->id?$reff->id:''}}" hidden>
    <div class="row">
        <div class="col">
            <label for="form-control">Kode Referral</label>
            <input type="text" name="kode" id="kode" class="form-control" value="{{$reff->code?$reff->code:''}}">
            @error('kode')
            <p class="text-danger">{{$message}}</p>
            @enderror
        </div>
        <div class="col">
            <label for="form-control">URL <small>spesial karakter akan di rubah ke (-) atau (_)</small></label>
            <input type="text" name="url" id="url" class="form-control" value="{{$reff->url?$reff->url:''}}">
            @error('url')
            <p class="text-danger">{{$message}}</p>
            @enderror
        </div>
    </div>
    <button class="button button-primary">Set</button>
</form> --}}